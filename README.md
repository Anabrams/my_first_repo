#terminal steps for creating a repository
#Step one: open terminal and make sure its in the correct directory --> 'pwd'
#step 2: initialize repo, tell gitbash to rrack. terminal command --> 'git init'
#step 3: create online repo to connect with
#step 4: name and press create repository
#step 5: cooy `git remote add origin' command and paste in vsc terminal 
1. Open the terminal and make sure it is in the correct directory
2. Intialize repo, tell gitbash to track. 
    - Terminal command: `git init` <- git initialize
3. go to github.com and click `new repo` 
4. give it a name and hit `create repo`
5. copy link into terminal 
#everytime you want to save new data into your github follow these 3 steps
6. in terminal `git add . `
7. in terminal `git commit -m "name of anything"
8. in termial `git push origin main` ---> this will push all commits to github